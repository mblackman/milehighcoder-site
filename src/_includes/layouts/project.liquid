---
layout: layouts/default.liquid
---

<div class="prose max-w-none w-full border-solid border-b border-base-content/20 pb-6 animate-fade-in-up">
  	<h1>{{ title }}</h1>
	<div class="flex text-sm font-light dark:font-extralight">
		<div class="flex">
        	{{ date | date : "%b %Y" }} - {% if activeDevelopment %}Present{% else %}{{ endDate | date : "%b %Y" }}{% endif %}
		</div>
	</div>
	{% if repo %}
	<div class="mt-2">
		<a href="{{ repo }}">
			{{ repo }}
		</a>
	</div>
	{% endif %}
  	<div class="mt-6">
  	{{ content }}
	</div>
</div>
